# 프로젝트 작업 규칙

## 기본 브랜치

- 사용자의 별도 브랜치 지시가 없으면 `main` 브랜치에서 작업·커밋·푸시한다.
- 사용자가 다른 브랜치를 명시하면 해당 지시를 우선한다.

## 작업 기록

- 작업 시작 시 `.agents` 폴더가 없으면 생성한다.
- `.agents/progress.md`에 작업 목표, 상태, 주요 변경·검증, 다음 단계 또는 차단 요인을 간결히 갱신한다.
- 완료하거나 중단할 때 `.agents/log.md`에 날짜·시간, 요청 요약, 수행 내용, 변경 파일, 검증 결과, 최종 상태를 기록하고 기존 기록을 보존한다.
- API 키, 비밀번호, 토큰, 개인 식별 정보 등 민감 정보를 기록하지 않는다.
